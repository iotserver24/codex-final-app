# Environment variables needed for codex local development.
# To use, copy this file to a new file named ".env" and fill in your private keys and settings.
# Your actual .env file should NOT be committed.

# AI Provider API Keys(Optional)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=


# Local AI Model Configuration (Optional)
# Set these if you are running local AI models like Ollama or LM Studio.
# Default for Ollama is http://127.0.0.1:11434
OLLAMA_HOST=

# GitHub Integration (Optional)
# Needed for features that interact with GitHub repositories.
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GITHUB_TOKEN=


# Apple Notarization (macOS Build Only)
# Only required if you are building and signing a release version for macOS.
APPLE_ID=
APPLE_PASSWORD=
APPLE_TEAM_ID=
SM_CODE_SIGNING_CERT_SHA1=


# Development & Testing Variables (Advanced)
# These are typically not needed for standard contribution.
# NODE_ENV=development
# E2E_TEST_BUILD=
# CI=
# DYAD_ENGINE_URL=
# DYAD_GATEWAY_URL=

# European Swallow AI
# Obtain an API key from https://api.europeanswallowai.com
EUROPEAN_SWALLOW_AI_API_KEY=
VITE_POLAR_CHECKOUT_URL
# Share Preview via E2B (Maintainer-only)
# Your app ships with YOUR E2B key. Users do not provide an E2B key.
# Set this only as the maintainer to enable Share Preview in sandbox.
E2B_API_KEY=

# Polar Billing (Maintainer-only)
# Optional if you want to verify paid durations (30/60m) via Polar.
# Users provide ONLY a Polar license key inside the Share panel.
# POLAR_CHECKOUT_URL can be a Polar “Checkout Link” to open Buy or QR.
POLAR_CHECKOUT_URL=
POLAR_API_KEY=